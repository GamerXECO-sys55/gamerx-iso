# GamerX OS · live-environment Hyprland tweaks
# Sourced last via the user-overrides include in /etc/skel hyprland.conf.

# Bigger desktop icons + clearer spacing for first-time users.
exec-once = notify-send "GamerX OS · Live" "Click the desktop installer icon or run 'sudo calamares' to install."
