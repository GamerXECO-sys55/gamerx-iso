# GamerX OS · Plymouth daemon config
# Forces theme=gamerx on every boot so we never fall back to the default
# spinner (which is what was causing the 1-2s flash).
[Daemon]
Theme=gamerx
ShowDelay=0
DeviceTimeout=8
